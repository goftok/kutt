{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node"
    }
  ],
  "scripts": {
    "install": "npm install --legacy-peer-deps",
    "build": "npm run build"
  },
  "routes": [{ "src": "/(.*)", "dest": "/" }]
}
